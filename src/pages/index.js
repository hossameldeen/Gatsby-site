import * as React from "react";
import { Link } from "gatsby";

const IndexPage = () => {
  return (
    <Link to="/another-page">Click to go to /another-page</Link>
  );
};

export default IndexPage;
